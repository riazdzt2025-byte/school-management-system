▶ নতুন সেশন — প্রম্পট ১৯ / ২৮ (সেশন DB-03) · উপ-ধাপ ১গ · Accounts/Finance guard matrix

আমি Habib। school-management-system প্রজেক্ট, নতুন এজেন্ট-সেশন, আগের কথোপকথনের কিছু মনে নেই।

১. প্রথমে পড়ো: docs/claude-rules.md, docs/prompts/prompt-19-db-03-permissions-data-isolation.md,
   docs/prompts/reports/DB-03.md (ধাপ ১ক, ১খ কী পাওয়া গেছে দেখে নাও)।

২. সেটআপ: git clone --depth 1 -b feature/riondev-branding <repo-url>, তারপর
   arena/01a0b7f7-school-management-system branch checkout করো, আগের commit ধরে এগোও (git log
   দিয়ে যাচাই করো; না থাকলে থামো)।

৩. স্কোপ (শুধু "Accounts/Finance" route-গুলো):
   add_money_receipt, edit_money_receipt, delete_money_receipt, money_receipt_list,
   add_voucher, edit_voucher, delete_voucher, voucher_list,
   add_salary_sheet, edit_salary_sheet, delete_salary_sheet, salary_sheet_list,
   finance_dashboard, accounts_approve_payment, accounts_admission_queue।

   প্রতিটার জন্য login/permission (Accounts department)/institution-scope guard কোড থেকে যাচাই
   করো। বিশেষ মনোযোগ: টাকা-সংক্রান্ত (receipt/voucher/salary) write route-গুলোয় cross-institution
   write টেস্ট (অন্য institution-এর object edit/delete করার চেষ্টা করলে 404/blocked হওয়া উচিত)।

   ফাঁক পেলে smallest fix + negative test (test_institution_write_isolation.py-তে যোগ করো)।

৪. সীমা: prompt-19 §৪ হুবহু।

৫. যাচাই: python manage.py test students.test_institution_isolation
   python manage.py test students.test_institution_write_isolation
   python manage.py check

৬. শেষে: docs/prompts/reports/DB-03.md-এ "### ১গ. Accounts/Finance" সেকশন যোগ করো (append)।
   patch বানিয়ে present_files দিয়ে দেখাও, push কোরো না। পরের ধাপ DB-03-1d (HR) — নিজে থেকে শুরু
   কোরো না।
