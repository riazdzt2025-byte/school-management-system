# EX-04 রিপোর্ট — প্রম্পট ০৫/২৮ · নতুন subject / Higher Math workflow

_তারিখ: ২০২৬-০৯-২৩ · Branch: `arena/01a0cd35-school-management-system` · Base: `974d616` = `origin/main` (PR #44 MERGED — EX-03 re-verify, 715+14) · Commit: `974d616` (no new commit — regression-only pin) · PR: #44 (MERGED, baseline) — no new PR for code; docs-only pin on this branch_

**সেশন-ধরন (owner FINAL সিদ্ধান্ত):** EX-04 **regression-only**। Higher Math / নতুন subject workflow ইতিমধ্যেই **Complete (baseline verdict)** — নতুন migration/JS নেই। শুধু তিনটি নির্দিষ্ট কমান্ড চালিয়ে ফল **pin** করা। Development স্পেক (prompt-05 §৩–§৪-এর fallback warning / inactive-subject নীতি / three-layer consistency test নতুন করে লেখা) এই সেশনে প্রযোজ্য নয় — owner FINAL সিদ্ধান্ত সেগুলো override করেছে এবং baseline-এর ওপর regression যাচাই নির্ধারণ করেছে।

## ১. আগের অবস্থা → এখনকার অবস্থা

**আগে (baseline — PROGRESS row 05 + reports/০০-baseline.md):** Higher Math তিন-স্তর (curriculum `curriculum_data.py:42,135,179` · migration `0042` · `SubjectRequirement` rows) ইতিমধ্যেই যাচাই-করা Complete; নতুন subject workflow (`test_new_subject_result_workflow.py` ৪৭, `test_result_analysis.py`) baseline-এ pass ছিল।

**এখন (২০২৬-০৯-২৩, session `arena/01a0cd35-school-management-system`, HEAD `974d616` = `origin/main` after PR #44 MERGED):** Owner FINAL সিদ্ধান্ত অনুযায়ী EX-04 শুধু **regression re-verify**। কোনো কোড/migration/JS পরিবর্তন হয়নি। তিনটি নির্দিষ্ট কমান্ড নিজে চালিয়ে baseline পুনরায় প্রমাণ করা হয়েছে এবং তিন-স্তরের proof (read-only grep) দিয়ে pin করা হয়েছে।

## ২. প্রমাণ (এই sandbox-এ চালানো কমান্ড ও ফল)

লোকাল env: fallback — Python 3.11.2 + Django **5.2.17** (isolated venv `/tmp/audit_venv`) + SQLite throwaway test DB (কোনো Postgres নেই; `DATABASES` default sqlite)। `makemigrations --check` leaf = **0046** (clean)।

| কমান্ড | ফল |
|---|---|
| `python manage.py test students.test_new_subject_result_workflow` | `Found 47 test(s)` · `Ran 47 tests in 21.049s` · **OK** |
| `python manage.py test students.test_result_analysis` | `Found 38 test(s)` · `Ran 38 tests in 2.551s` · **OK** |
| `python manage.py check` | `System check identified no issues (0 silenced)` |
| `python manage.py makemigrations --check --noinput` | `No changes detected` · exit 0 |

### তিন-স্তরের Higher Math proof (read-only, no code change)
1. **Curriculum** — `students/curriculum_data.py`:
   - `:42` `('HMATH', 'Higher Mathematics', 100, 'OPTIONAL')` (default def)
   - `:135` SSC `SCI`: `('HMATH', 'MANDATORY', '', '')`
   - `:179` HSC `SCI`: `('HMATH', 'OPTIONAL', 'sci_4th', '')`
2. **Migration** — `students/migrations/0042_higher_math_mandatory_science.py`: `make_ssc_science_higher_math_mandatory` → SSC `admission_class in ['9','09','10']` + `group='SCI'` + `subject__code='HMATH'` → `MANDATORY`; `reverse_ssc_science_higher_math` → `OPTIONAL`/`sci_4th` (data operation, দ্বি-মুখী)।
3. **Assigned** — `SubjectRequirement` rows (SSC 9/09/10 SCI HMATH = MANDATORY) — `test_new_subject_result_workflow` + `test_result_analysis` মডিউলের ৪৭+৩৮ = **৮৫ focused test** দিয়ে cover হয়।

## ৩. পরিবর্তিত ফাইল

- `docs/prompts/PROGRESS.md` — row ০৫ স্ট্যাটাস `⏳ অপেক্ষমাণ` → `✅ সম্পন্ন` (regression pin) + তারিখ/commit/PR/টেস্ট-প্রমাণ।
- `docs/prompts/reports/EX-04.md` — এই রিপোর্ট (নতুন)।
- কোনো source / migration / JS পরিবর্তন হয়নি।

## ৪. যাচাই হয়নি এমন অংশ (Unverified)

- শুধু **regression-only** scope অনুযায়ী চালানো হয়েছে — prompt-05 §৩-এর নতুন development আইটেম (fallback UI warning/notice, inactive-subject ExamMark নীতি, optional/conditional `not_applicable` behavior, নতুন three-layer consistency test) এই সেশনে করা হয়নি (owner FINAL: baseline ইতিমধ্যে Complete, নতুন migration/JS নেই)।
- Postgres:16 matrix + Node CI এই sandbox-এ চালানো হয়নি (নেই); baseline-এ CI pass ছিল (PR #44)।
- Live/production DB, Render, S3/cron — স্পর্শ করা হয়নি (owner নীতি)।

## ৫. বাকি ঝুঁকি / পরবর্তী কাজ

- prompt-05 §৩-এর development আইটেম (fallback notice, inactive-subject নীতি, optional `not_applicable`) যদি ভবিষ্যতে চাওয়া হয়, তখন আলাদা সেশনে করতে হবে — এই regression pin তাতে বাধা নয়।
- `test_subject_assignment_office` (Office CRUD/inline new-subject) baseline-এ Complete; এই session-এ আলাদা চালানো হয়নি কিন্তু owner নির্দিষ্ট ৩ কমান্ডের বাইরে — প্রয়োজনে পরবর্তী regression-এ অন্তর্ভুক্ত করা যেতে পারে।

## ৬. owner-সিদ্ধান্ত

- **Owner FINAL (এই সেশন):** EX-04 = **regression-only**; Higher Math workflow ইতিমধ্যে Complete (baseline verdict); নতুন migration/JS নেই; শুধু তিনটি কমান্ড চালিয়ে pin। অনুসরণ করা হয়েছে।
- prompt-05 §৮-এর ৩টি প্রশ্ন (inactive-subject ExamMark নীতি / optional choice না থাকলে behavior / class-9-এর নিচে grouping) এই regression-only session-এ প্রযোজ্য নয় (development আইটেম owner FINALLY বাদ দিয়েছেন)।
