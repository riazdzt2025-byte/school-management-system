# EX-01 রিপোর্ট — প্রম্পট ০২/২৮ · Import redirect ও Analysis subtab

_তারিখ: 2026-09-19 · Branch: `arena/01a0ba60-school-management-system` · Base: origin/main `f03a16d` (PR #38 merge-পরবর্তী) · Commit: `b255fcd` · PR: **#39 (OPEN — owner merge-অনুমোদন বাকি)**_

> প্রম্পট-ফাইলে (`prompt-02-…`) branch হিসেবে `arena/01a0b7f7-school-management-system` লেখা ছিল; সেটি পুরোনো সেশন-প্ল্যানের উদ্দেশ্যে লেখা fixed নাম। এই সেশনের নির্ধারিত branch `arena/01a0ba60-…` — প্ল্যানের অন্য নিয়ম (ক্রম, PR, merge-নিষেধ) হুবহু মানা হয়েছে।

## ১. আগের অবস্থা → এখনকার অবস্থা

| চাহিদা | আগে (baseline, `০০-baseline.md` §EX-01) | এখন | প্রমাণ |
|---|---|---|---|
| (ক) সফল import-এ একই exam+subject(+group)-এর import পেজে ফেরা | আচরণ ছিল (`views.py:3803-3810`, comment "Stay on the same import page … (E1)"), test শুধু subject-পর্যন্ত ছিল (`tests.py:2915`) | আচরণ অপরিবর্তিত; **group param সংরক্ষণ** regression test-এ pin করা | নতুন `test_import_stay_on_page_preserves_subject_and_group` (`students/tests.py`, `GPAPaginationAndAbsentDisplayTests`) — POST(git group=SCI + subject + Excel) → 302, URL-এ `subject=<pk>` ও `group=SCI` |
| (খ) Exam সেকশনের ভেতরে Analysis subtab | ছিল না — Exam flyout-এ ৩টি link, আলাদা "Result Analysis" group (`base.html:233-243`) | Exam flyout-এ nested **Analysis** ব্লক — একই ৫টি named URL, `can_result_analysis` gate; পুরোনো group অপরিবর্তিত ⇒ **দুটি entry point, একটি named URL** | `base.html` Exam flyout; test `test_exam_flyout_lists_analysis_links_for_authorised_user` (প্রতি URL `href` ঠিক ২ বার) |
| (গ) Cross-link | ছিল না | `exam_list`-এ দৃশ্যমান `analysis-entry` বার (৫ link, gated) + ৫টি result পেজের হেডারে প্রাসঙ্গিক link (`analysis-jump`): result_sheet→Subject Fail List · exam_result_summary→Result Cards (Class) · full_rank_list→Merit Slides · top_10→Merit Slides · student_result_detail→Multi-term Results | `test_exam_list_shows_analysis_entry_…`, `ResultPageAnalysisCrossLinkTests` (৫টি anchor-ম্যাচ টেস্ট + unauthorized-এ অনুপস্থিতি) |
| (ঘ) dead link / permission ফাঁক নেই | per-view guard আগে থেকেই ছিল | **অপরিবর্তিত**: guard `_require_result_analysis_department` অক্ষত; মেনুতে শুধু সে-ই link দেখে যে view চালাতে পারে; direct URL আচরণ আগের মতো | `test_anonymous_user_is_redirected_to_login` (৫×302), `test_guard_still_blocks_users_without_department` (no-perm→403, Accounts+view_student→403), `test_authorised_user_reaches_every_analysis_page` (৫×200) |

**ইচ্ছাকৃতভাবে করা হয়নি:** নতুন view/route/template-page নেই (৫টি existing named URL পুনব্যবহার); পুরোনো "Result Analysis" group সরানো হয়নি (owner-ডিফল্ট = রাখা); navigation JS-এ কোনো পার্থক্য নেই (CSS-only subhead); কোনো model/migration নেই; public পেজে কোনো internal link যোগ হয়নি।

## ২. প্রমাণ (এই sandbox-এ চালানো কমান্ড ও ফল)

লোকাল env: README-নথিভুক্ত fallback — Python 3.11.2 + Django 5.2.17 + sqlite (isolated venv `/tmp/ex01_venv`)।

| কমান্ড | ফল |
|---|---|
| `python manage.py test students.test_published_lock_and_cell_shortcut` | **Ran 7 tests — OK** |
| `python manage.py test students.test_result_analysis` | **Ran 38 tests — OK** |
| `python manage.py test students.test_navigation` | **Ran 17 tests — OK** (৫ পুরোনো + ১২ নতুন) |
| `python manage.py check` | System check identified no issues (0 silenced) |
| `python manage.py makemigrations --check --dry-run` | No changes detected |
| `python manage.py test students` (পূর্ণ suite) | **Ran 638 tests — OK**, 0 fail / 0 error (baseline 625 + নতুন ১৩) |
| `node --test students/js/*.test.js` (Node 22.22.3) | **14 pass, 0 fail** |

নতুন ১৩টি test: import group-preservation ১ + `ExamAnalysisSubtabTests` ৭ + `ResultPageAnalysisCrossLinkTests` ৫।

**CI (authoritative, Django 6.1 / Python 3.12) — PR #39: তিনটি check-ই pass।**

| Check | ফল | সময় |
|---|---|---|
| `test (sqlite, 3.12)` | **pass** | 6m28s |
| `test (postgres, 3.12, true)` | **pass** | 5m12s |
| `Node row-action button tests` | **pass** | 5s |

## ৩. যাচাই হয়নি

- **live Render** — merge-এর পর auto-redeploy হলেও sandbox থেকে live যাচাই সম্ভব নয় (owner spot-check: Exam flyout → Analysis)।
- ভিজ্যুয়াল রেন্ডারিং হাতে দেখা হয়নি (ব্রাউজার নেই); পরিবর্তন CSS-only + existing pattern-এর markup, Django test-এ rendered HTML assert করা।

## ৪. বাকি ঝুঁকি / উল্লেখ্য

- পুরোনো open **PR #23** (import redirect stay-on-page প্রস্তাব) — আচরণটি main-এ আগে থেকেই আছে এবং এখন group-preserving regression-ও pin হয়েছে ⇒ owner চাইলে PR #23 supersede ঘোষণা করে বন্ধ করতে পারেন (এই সেশনে বন্ধ করা হয়নি — owner-এর সিদ্ধান্ত)।
- Subtab hover/flyout ব্যবহার existing `nav-group`/`flyout` প্যাটার্নে, নতুন JS নেই; mobile (≤768px)-এ tap-toggle existing handler-ই কাজে।
- দুটি entry point-এ একই লিখিত লেবেল রাখা হয়েছে যাতে ব্যবহারকারী বিভ্রান্ত না হন (owner-সিদ্ধান্ত চাইলে groupটি পরে সরানো যাবে — কোড-পরিবর্তন ছোট)।

## ৫. owner-সিদ্ধান্ত (§৮) — কীভাবে নেওয়া হলো

1. **Placement** — ডিফল্ট অনুসরণ: Exam flyout-এ **সরাসরি ৫টি link** (নতুন landing view তৈরি হয়নি)।
2. **পুরোনো group** — সুপারিশ অনুসরণ: **রাখা হয়েছে**; দুটি entry point একই named URL-এ যায় (test-এ pinned)।
3. **এই PR merge** — owner-এর অনুমোদনের অপেক্ষায়; এজেন্ট merge করবে না।

## ৬. পরের প্রম্পট

প্রম্পট ০৩/২৮ (EX-02 — Result/Register roll-order): baseline অনুযায়ী register/roll order বড় output-গুলোতে ✅; বাকি = `exam_result_summary` / `class_section_summary` / attendance class-wise list / print CSS-এর নিয়ম + নিয়ম-ডক। START-ব্লক: `docs/prompts/copy-paste/kickoff/prompt-03-START.txt`। কেউ নিজে থেকে শুরু করবে না — owner ক্রমিকভাবে দেবেন।
