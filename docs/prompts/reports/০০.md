# সেশন ০০ — Baseline যাচাই (প্রম্পট ০১)

_তারিখ: ২০২৬-০৯-১৯ · Branch: `arena/01a0b85c-school-management-system` · Commit: baseline doc update_

## কমান্ড ফল (প্রমাণ)
- `python manage.py check` → 0 issues
- `python manage.py check --deploy` → 6 warnings (W004/W008/W009/W012/W016/W018) — expected dev warnings
- `python manage.py makemigrations --check` → No changes detected (leaf 0043)
- `python manage.py test students` → **Ran 625 tests — OK (228s)** (was 555 at 44cbcc3; +70)
- `node --test students/js/*.test.js` → **14 pass / 0 fail** (was 6; +8 shortcut)
- `git status -sb` → working tree clean; `HEAD = 8b7aa62 = origin/main` (PR #35)

## Verdict summary (২৮-প্রম্পট)
- ⏭️ ইতিমধ্যে সম্পন্ন (verify-only): EX-02 roll-order, EX-04 Higher Math, EX-06 GPA boost, OF-01 guardian contact, OF-02 pagination 100, OF-03 subject assignment subtab, OF-04 admission funnel, DB-03 isolation, DB-05 backup tooling
- 🟡 Partial (verify-first + ছোট কাজ): EX-01 (Analysis Exam-subtab), EX-05 (AB/dash policy), EX-07 (shortcut page coverage), OF-05 (admission photo, purge), OF-06 (progress tracking page), OF-07 (capacity/duplicate policy), OF-08 (TC/cert/promotion guards), DB-01 (nav check), DB-04 (deploy settings), DB-06 (restore drill/automation), AT-01 (attendance correction), EM-03 (payroll lock)
- ❌ Missing (build needed): EX-03 (SubjectMarkSetting.group field), DB-02 (developer branding — owner decision প্রথমে), AT-02 (calendar view), EM-01 (teacher assignment), EM-02 (leave — owner decision ছাড়া শুরু নয়)
- ⏳ Pending: FN-01 (depends on 01-27)

## উল্লেখযোগ্য পরিবর্তন (পুরোনো দাবি vs এখন)
- D-GPA boost (4.90→5.00) ইতিমধ্যে `result_utils.py:928-931`-এ আছে → EX-06 verify-only।
- Import stay-on-page redirect `import_exam_marks` L3731/3802-এ কাজ করছে → EX-01 stay-on-page done; Exam-subtab link বাকি।
- Migration leaf 0043 (auditlog_institution), not 0042.
- Test counts 625/14 (baseline), 555/6 নয়।

## যাচাই হয়নি
- Production/Render state (UNKNOWN — নিয়ম অনুযায়ী ছোঁয়া হয়নি)
- CI status (PR খুলে GitHub Actions-এ verify হবে)
- Python 3.12+Django 6.1 compatibility (CI-তে চলবে)

## SSC/BoardResult
RetiredBoardFeatureTests pass (625-এর অংশ); restore করা হয়নি।

## Owner-সিদ্ধান্ত
এই সেশনে কোনো নতুন সিদ্ধান্ত নেওয়া হয়নি — প্রম্পটের §৭-তে তালিকাভুক্ত সিদ্ধান্তসমূহ সংশ্লিষ্ট প্রম্পটে উত্থাপন করা হবে।

## ঝুঁকি
- prompts plan-এর ভেতরের কয়েকটি দাবি পুরোনো (EX-01 stay-on-page, EX-06 GPA boost) — আজকের baseline অনুযায়ী ledger-এ ⏭️ চিহ্নিত করা হয়েছে; subsequent prompt-গুলো verify-first পদ্ধতিতে যাবে।
