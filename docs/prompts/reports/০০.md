# সেশন ০০ — প্রম্পট ০১ / ২৮ (baseline) · সংক্ষিপ্ত রিপোর্ট

_তারিখ: ২০২৬-০৯-১৯ (UTC) · branch `arena/01a0b9da-school-management-system` · base `8b7aa62` = `origin/main`_
_বিস্তারিত matrix: [`০০-baseline.md`](০০-baseline.md) · ledger: `docs/prompts/PROGRESS.md` সারি ০১_

## ১. আগের অবস্থা → এখনকার অবস্থা

- **আগে:** `main`-এ ledger অনুযায়ী কোনো প্রম্পট চালানো হয়নি; `docs/PROJECT_STATUS.md` (২০২৬-০৯-১৭,
  `44cbcc3`) এখনো বলত "GPA boost নেই", "pagination নেই", "import stay-on-page Partial" — কোড এর
  মধ্যে বদলেছে, ডক পিছিয়ে ছিল। ৬২৫ + ১৪ টেস্টের দাবি এই checkout-এ কেউ চালিয়ে দেখেনি।
- **এখন:** পুরো suite + check + backup drill নিজে চালিয়ে সংখ্যা নিশ্চিত (**৬২৫ Django + ১৪ Node সব pass**),
  ২৮-সেশনের প্রতিটি আইটেম verdict-সহ চিহ্নিত (Complete ৯টি+অর্ধেক দুটি, Partial ৭, Missing ৩,
  Unverified ৪), ডক-এর পুরোনো দাবি সংশোধিত।

## ২. প্রমাণ (চালানো কমান্ড + প্রকৃত ফল)

| কমান্ড | ফল |
|---|---|
| `python manage.py check` | `0 issues` |
| `python manage.py check --deploy` | ৬ warning (`W004 W008 W009 W012 W016 W018`) — প্রত্যাশিত dev expectation, exit 0 |
| `python manage.py makemigrations --check` | `No changes detected` (leaf `0043_auditlog_institution`) |
| `python manage.py test students` | **Ran 625 tests in 231.431s → OK** |
| `node --test students/js/*.test.js` | **14 pass / 0 fail** (৮ + ৬) |
| `./scripts/backup_smoke_test.sh` | **১৫ step pass / ০ fail** (sqlite, plaintext+encrypted, SHA verify) |
| `moto_server` + `backup_smoke_test.sh --s3-endpoint …` | **২৮ step pass / ০ fail** (off-box upload → fetch → restore → retention ২) |

Env: Python 3.11.2 + isolated venv `/tmp/audit_venv`, Django **5.2.17** (README-এর fallback; `requirements.txt`-এর 6.1 = Python 3.12+)।

## ৩. পরিবর্তিত ফাইল

- `docs/prompts/reports/০০-baseline.md` (নতুন — পূর্ণ verdict matrix)
- `docs/prompts/reports/০০.md` (এই ফাইল)
- `docs/prompts/PROGRESS.md` (সারি ০১ + ২৭টি সারির baseline নোট)
- `docs/PROJECT_STATUS.md` (dated Verification section + পুরোনো ভুল দাবি সংশোধন)
- `docs/TASK_BACKLOG.md` (remaining list সংশোধন + update log)
- `docs/HANDOFF.md` (নতুন সেশন-নোট)

**কোনো** feature code / migration / template / JS বদলানো হয়নি (prompt §২-এর সীমা)।

## ৪. যাচাই হয়নি (Unverified)

- live Render config/cron/off-box/alert/S3 bucket/production DB engine — credential ছাড়া অসম্ভব (owner-only)।
- postgres:16-এ suite — লোকালে Postgres নেই; CI matrix-এ প্রমাণ হবে (`gh pr checks` ফেলে দেখা হবে)।
- OF-07/OF-08-এর integrity audit ও FN-01 — যথাক্রমে ১৫/১৬/২৮ নম্বরের সেশনের কাজ।

## ৫. বাকি ঝুঁকি

- ২৭টি prompt-এর প্রেক্ষাপট এখনো `f64194a`-ভিত্তিক লেখা — সেশনগুলো baseline (§৪ matrix) দিয়ে
  নিজেদের দাবি সংশোধন করে নেবে, নাহলে অপ্রয়োজনীয় কাজ হবে।
- **PR #36 ও #37** (সমান্তরাল "প্রম্পট ০১" সেশন, ভিন্ন branch) খোলা — দুটো merge করলে ডকে
  পরস্পরবিরোধী verdict বসবে।

## ৬. owner-সিদ্ধান্ত

| প্রশ্ন | কেন দরকার | সুপারিশ |
|---|---|---|
| OPEN PR #36 / #37 / #38-এর মধ্যে কোনটি merge? | তিনটিই একই baseline স্কোপ, একই ডক ফাইল ছোঁয় | **একটি** merge করুন — এই PR (#38) স্বতন্ত্রভাবে চালানো কমান্ড-ফলসহ প্রমাণ দেয় |
| EM-02 (leave) নীতি লিখে দেবেন কি? | prompt ২৬-এর শর্ত: নীতিহীন কোড নিষিদ্ধ | ২৬ নম্বর সেশনের আগে `reports/EM-02-decision-request.md` ভিত্তিতে নীতি দিন |
| DB-04-এর HSTS / SSL-redirect / secure-cookie (live) | `check --deploy`-এর ৬ warning-এর মূল কারণ | live-এ চালু করতে চাইলে DB-04 সেশনে বলুন; dev-এ থাকলে এখনকার অবস্থা সংরক্ষণ |

বাকি সিদ্ধান্ত-সারি `docs/prompts/README.md` §৭ (EX-01, EX-03, EX-06, EX-07, OF-02, OF-05, OF-06,
OF-07, OF-08, AT-01, AT-02, EM-01, EM-03) — সেশন আসার আগে দিলে সেই সেশন দ্রুত এগোবে।
