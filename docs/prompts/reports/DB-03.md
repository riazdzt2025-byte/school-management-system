# DB-03 - Permissions and data isolation (part 1: route guard matrix)

Date: 2026-09-21. Base: main at 67956c1.

## What was checked

All 108 named routes (216 requests, GET and POST) were requested over real HTTP
as an anonymous visitor.

| Result | Count |
|---|---|
| Refused (302 to login) | 207 |
| Public on purpose (200) | 6 requests / 3 routes |
| Not applicable (logout GET 405, media 404) | 3 |

Public routes, reviewed: `login`, `public_admission_apply` (rate limited),
`admission_dropdown_options` (JSON with group names and section letters only).

Pinned by `students/test_route_guards.py`: any new route that answers an
anonymous visitor with anything but a refusal fails the test. The test was
verified to fail when an unguarded route is added.

The login gate is pinned too: a non-admin user needs an active
`InstitutionAccess` row for the chosen institution and department.

## Findings needing an owner decision (1 resolved, 1 still open)

1. **Read access across departments — RESOLVED, restricted (2026-09-21).**
   Money receipts, vouchers, salary sheets and the finance dashboard are now
   gated behind `students.view_moneyreceipt` / `view_voucher` /
   `view_salarysheet`, granted only to the Accounts group. Office/Exam clerks
   get 403 and the sidebar "Accounts" nav-group is hidden for them; the
   employee list and class/section summary were left open (only the four
   money/salary pages were flagged as sensitive). Pinned by
   `students/test_finance_department_gate.py`. See SEC-FU-3 below.
2. **Users without an access row are unrestricted in the scoping helpers**
   (`_institutionally_scoped` in `views.py`). This is safe only because
   `/login/` refuses such users (pinned above) and `/admin/` needs `is_staff`.
   Hardening option: deny by default. It touches many tests, so it is a
   separate task.

## Follow-ups

- SEC-FU-1 (done): counters can now live in a shared database table with
  `RATE_LIMIT_CACHE=db`. Default is unchanged (per-process memory). See
  DEPLOY_NOTES.md and `students/test_rate_limit_cache.py`.
- SEC-FU-2 (decided, no behaviour change): login mirrors only the Office, Exam
  and Accounts access rows into groups. HR, Subjects and Audit membership of a
  non-admin clerk is removed at login; admin/staff keep those groups. Pinned by
  `students/test_department_group_sync.py`. Alternative if wanted later: add
  HR and Audit as login departments (changes `InstitutionAccess` choices, the
  login page and needs a migration).
- SEC-FU-3 (done, 2026-09-21): money_receipt_list, voucher_list,
  salary_sheet_list and finance_dashboard now require
  `students.view_moneyreceipt` / `view_voucher` / `view_salarysheet`
  (`permissions.py` Accounts group gained the `view` action on those three
  models; `students/apps.py`'s `post_migrate` hook re-syncs group permissions
  on every `migrate`, so no data migration was needed). Sidebar links hidden
  the same way. Pinned by `students/test_finance_department_gate.py`.
- Finding 2 above (deny-by-default for access-less users) is still open —
  owner has not yet chosen whether to do it now or later; no code changed.
