# EX-03 রিপোর্ট — প্রম্পট ০৪/২৮ · Group-based Mark Evaluation

_তারিখ: ২০২৬-০৯-২০ · Branch: `arena/01a0bd0d-school-management-system` · Base: `7a13e33` = `origin/main` (PR #41 merge-পরবর্তী) · Commit: (পরবর্তী) · PR: (পরবর্তী)_

> এই সেশনের নির্ধারিত branch `arena/01a0bd0d-…` — প্রম্পট-ফাইলে `arena/01a0b7f7-…` লেখা ছিল (পুরোনো প্ল্যানের fixed নাম); বাকি সব নিয়ম হুবহু মানা হয়েছে।
> **owner-সিদ্ধান্ত (§৮, এই সেশনের শুরুতে জিজ্ঞেস করে নেওয়া):** (১) parts-যোগ **কঠোর সমান** (`sum(parts)==full_marks`); (২) Weekly Test **শুধু MID_TERM_1/2/3-এ**; (৩) group-specific config **শুধু 9–12-এ** (নিচের class-এ group খালি বাধ্যতামূলক)।

## ১. আগের অবস্থা → এখনকার অবস্থা

**আগে:** `SubjectMarkSetting` = `institution + admission_class + subject + exam_type` unique; **group field নেই**। একই class + exam_type-এ SCI/ARTS আলাদা full marks / parts / pass % দেওয়া অসম্ভব — দ্বিতীয় row unique-ভঙ্গ (IntegrityError) হতো। `get_subject_marks(exam, subject)` শুধু class+exam_type দেখে, group অগ্রাহ্য; `active_exam_subject_ids` group-অজ্ঞ; marks entry/import ও result একই config পেত, group-ভেদ বোঝাত না। `mark_evaluation_settings` UI-এ group selector ছিল কিন্তু save-এ group ignore হতো; validation (parts exact/weekly/group) ছিল না; `mark_entry` group-mismatch subject লুকাত কিন্তু marks config group-ভেদ মানত না।

**এখন:** পুরোপুরি **group-aware** (EX-03):
- **Model:** `SubjectMarkSetting.group` (`CharField(choices=GROUP_CHOICES, blank=True, default='')`, help: blank = সব group-এ default) + uniqueness `institution+admission_class+subject+exam_type+group` (নতুন migration **0044** remove old → add field → alter pass%/full_marks validator → add new constraint). `clean()` → group blank for <9, pass 0–100, full>0; parts/weekly model-এ permissive (legacy mismatched allow, view-এ strict); `save()` → <9 হলে group clear + `full_clean()`.
- **Resolution chain (এক ফাংশন):** `result_utils.get_subject_marks(exam, subject, group=None)` → effective_group → group-specific row → blank-group default → Subject global defaults (class zero-padding tolerant, exam.spelling preferred). `resolve_mark_setting()` helper একই chain low-level. `active_exam_subject_ids(exam, subject_ids, group=None)` → group-aware active check (exact group → blank fallback). `get_exam_subjects()` এখন group-aware active; `build_exam_results()`/`failed_subject_rows`/`section_arrangement` group param দিয়ে `get_subject_marks(..., group=group)` ও `ReligionColumn.header_marks_config(..., group)`.
- **Marks entry/import:** `import_exam_marks`, `enter_marks`, `download_marks_import_template` `get_subject_marks(..., group=selected_group)`; `build_subject_marks_workbook(..., group)` ও `parse_subject_marks_workbook(..., group)` group-aware; `result_sheet` sheet_columns header group-aware.
- **UI (`mark_evaluation_settings`):** group selector + per-group listing (exam_like without exam_type → inactive subjects still listed for re-enable); SAVE validation: full>0, pass 0–100, group rule (<9 blank), weekly only MID (error & skip), parts exact equality (error & skip, owner-সিদ্ধান্ত), duplicate (unique constraint → ValidationError), clear error messages; `record_audit` on save/update; institution scoping `_resolve_requested_institution` + `_scope_by_allowed_institutions` অপরিবর্তিত; weekly_test field MID-এই প্রাসঙ্গিক (অন্যথায় skip).
- **Backward compat:** পুরোনো row group='' → সব group-এ default হিসেবে কাজ করে; migration ডেটা হারায় না; বিদ্যমান 0043-পরবর্তী data ভাঙে না.

## ২. প্রমাণ (এই sandbox-এ চালানো কমান্ড ও ফল)

লোকাল env: fallback — Python 3.11.2 + Django **5.2.17** (isolated venv `/tmp/venv`) + SQLite

| কমান্ড | ফল |
|---|---|
| `python manage.py test students.test_group_based_mark_evaluation` (নতুন) | **Ran 15 tests — OK** |
| `python manage.py test students.test_result_analysis students.test_new_subject_result_workflow` | **Ran 85 tests — OK** |
| `python manage.py test students.test_result_analysis students.test_new_subject_result_workflow students.test_subject_assignment_office` | **85+28 — OK** |
| `python manage.py test students.tests.MarksPartsAndPassRulesTests students.tests.MarkEvaluationActiveSubjectTests` | **12 tests — OK** (পূর্বে mismatch/weekly/inactive fail ছিল → এখন model permissive + view strict → সব pass) |
| `python manage.py check` | System check identified no issues (0 silenced) |
| `python manage.py makemigrations --check` | No changes detected (migration **0044** generated) |
| `python manage.py test students` (পূর্ণ suite) | **Ran 668 tests — OK** (653 + 15 নতুন), 0 fail / 0 error |
| `node --test students/js/result_cell_shortcut.test.js students/js/student_row_actions.test.js` | **14 pass, 0 fail** |

নতুন ১৫টি test (`students/test_group_based_mark_evaluation.py`):
- group-specific override কাজ করে (SCI 70 vs BUS 100, explicit group param)
- blank-group fallback (no SCI row → 80 default) + Subject default fallback
- inactive subject group-aware exclude (SCI inactive → SCI-এ hide, BUS-এ show)
- parts sum exact validation (view-level reject mismatched, model permissive)
- pass % 0–100, full>0, weekly MID-only (view reject non-MID, MID pass)
- duplicate blocked (same group duplicate → ValidationError/IntegrityError, different group allowed)
- class<9 group blank must (SCI for 6 → ValidationError, blank save & auto-clear)
- class_supports_group helper (6/8 false, 9/09/10/12 true)
- institution scoping (other institution exam fallback to Subject, view not leaked)
- zero-padding class resolution (09 setting found via 9 exam)
- build_exam_results group resolution (SCI 80% pass vs BUS 40% pass)
- baseline deficiency proof (same class+subject+exam_type SCI vs BUS two rows coexist)

**CI (authoritative, Django 6.1 / Python 3.12) — PR (পরবর্তী):** `gh pr checks` — **sqlite + postgres:16 + Node** — তিনটি check pass প্রত্যাশিত (local 5.2.17-এ 668 pass ইতিমধ্যে প্রমাণিত; migration 0044 উভয় backend-এ valid)।

## ৩. যাচাই হয়নি / বাকি ঝুঁকি

- **ভিজ্যুয়াল UI** ব্রাউজারে হাতে দেখা হয়নি (sandbox-এ ব্রাউজার নেই) — group selector + per-group listing + weekly_test column + inactive tick সব কোড-স্তরে যাচাই; template-এ group badge আছে কিন্তু CSS/JS interaction হাতে দেখা বাকি।
- **Weekly Test UI** MID-এই শুধু দেখানো উচিত কিনা তা owner-সিদ্ধান্ত অনুযায়ী view-এ reject করা হয়েছে, কিন্তু template এখনো সব exam_type-এ weekly_test input দেখায় (প্রাসঙ্গিকতা কম, তবে save-এ block হয়; ভবিষ্যতে hide করা যেতে পারে)।
- **Postgres-এ লোকাল চালনা হয়নি** — `UniqueConstraint` with group + `nulls` handling উভয় backend-এ Django-সমর্থিত; CI postgres job এটিই প্রমাণ করবে।
- `mark_evaluation_settings` POST-এ `record_audit` আছে কিন্তু audit Log-এর group value live-এ যাচাই হয়নি (local test-এ call হয়, DB-এ row আছে)।
- Existing mismatched weekly_test rows (যদি থাকে) model-এ permissive থাকায় DB-এ থাকবে, কিন্তু view-এ save করতে গেলে reject হবে — historical data নিয়ে কোনো migration clean-up করা হয়নি (ইচ্ছাকৃত, backward compat)।

## ৪. owner-সিদ্ধান্ত (§৮) — কীভাবে নেওয়া হলো

সেশনের শুরুতে `ask_user` দিয়ে জিজ্ঞেস করা হয়:

1. Parts-এর যোগফল exact vs ≤ → **exact** (কঠোর সমান, সুপারিশ অনুসরণ) — view-এ `sum(parts)!=full` হলে error & skip, model permissive।
2. Weekly test কোন exam type → **MID_TERM_1/2/3 only** (সুপারিশ অনুসরণ) — non-MID exam_type-এ weekly_test থাকলে view error & skip; MID-এ 50+30+20=100 exact।
3. Group-specific config শুধু 9–12 → **হ্যাঁ** (সুপারিশ) — <9 class-এ group must be blank (clean ValidationError + save auto-clear)।

সবই code + test + doc-এ প্রয়োগ করা হয়েছে; weekly_test group-ভেদ আলাদা কিনা তা effective_group দিয়ে group-specific row-এ weekly_test থাকতে পারে (MID-এ SCI vs BUS আলাদা হতে পারে) — owner mid_only বেছে নেওয়ায় weekly_test শুধু MID-এ প্রাসঙ্গিক, group-ভেদে আলাদা config-ও MID-এই প্রযোজ্য।

## ৫. পরের প্রম্পট

প্রম্পট ০৫/২৮ (EX-04 — নতুন subject / Higher Math workflow): baseline **Complete** — যাচাই করা: ইতিমধ্যে সম্পন্ন — পরের প্রম্পট শুধু regression যাচাই করবে (curriculum_data 42,135,179; migration 0042; test 47)। **প্রস্তাব:** `python manage.py test students.test_new_subject_result_workflow students.test_result_analysis` + `check` + `makemigrations --check` চালিয়ে regression pin, কোনো নতুন migration/JS নেই।

## ৬. পরিবর্তিত ফাইল

- `students/models.py` — `SubjectMarkSetting.group` + updated Meta constraint + `MarksConfigMixin.pass_percentage` 0–100 + validators + `clean`/`save` group logic
- `students/migrations/0044_…py` — new append-only migration (group field + constraint)
- `students/result_utils.py` — `get_subject_marks(group)`, `resolve_mark_setting`, `active_exam_subject_ids(group)`, `get_exam_subjects` group-aware active, `ReligionColumn.header_marks_config(group)`, `build_exam_results` group pass-through
- `students/views.py` — `mark_evaluation_settings` group-aware fetch/save/validation/audit + `import_exam_marks`/`enter_marks`/`download_marks_import_template`/`result_sheet`/`_exam_result` group pass-through + `class_supports_group` import
- `students/marks_import.py` — `build_subject_marks_workbook`/`parse_subject_marks_workbook`/`parse_subject_marks_sheet` group param
- `students/test_group_based_mark_evaluation.py` — 15 নতুন test (group override, fallback, inactive, parts/pass/weekly validation, duplicate, <9 group, institution scoping, zero-pad, result resolution, baseline)
- `docs/prompts/reports/EX-03.md` (নতুন), `docs/prompts/PROGRESS.md` (সারি ০৪), `docs/PROJECT_STATUS.md` (E4), `docs/TASK_BACKLOG.md`, `docs/HANDOFF.md` (এই নোট)

## ৭. সীমা ও নিয়ম মান্য

- branch `arena/01a0bd0d-…` only, main-এ সরাসরি push নয়; PR খোলা হবে, owner merge করবেন (এজেন্ট merge নয়)
- কোনো `reset --hard` / `git clean` / force-push নয়; pre-existing uncommitted change নেই
- বিদ্যমান migration edit নয়, append-only 0044
- secret/PII commit নয়; test synthetic data only
- smallest coherent change: শুধু EX-03 scope (group field + resolution + validation + UI message), কোনো extra refactor/payment/SMS নয়

## ৮. যাচাই কমান্ড (পরবর্তী ডেভেলপারের জন্য)

```bash
python3 -m venv /tmp/audit_venv && . /tmp/audit_venv/bin/activate
pip install "Django>=5.2,<6" openpyxl Pillow python-dotenv dj-database-url whitenoise psycopg2-binary django-storages boto3
python manage.py test students.test_group_based_mark_evaluation --verbosity 2
python manage.py test students.test_result_analysis students.test_new_subject_result_workflow --verbosity 1
python manage.py test students.test_subject_assignment_office --verbosity 1
python manage.py check
python manage.py makemigrations --check
python manage.py test students --verbosity 1
node --test ./students/js/result_cell_shortcut.test.js ./students/js/student_row_actions.test.js
```
