__pycache__/
*.pyc
db.sqlite3
.env
staticfiles/
*.log
.DS_Store

db.sqlite3.backup
production_full_dump.json
production_students_dump.json

# Local scratch / generated artifacts
*.patch
temp*.json
temp_*.txt
test_*.txt
.venv/
venv/

# Backup / restore artifacts and disposable restore-drill scratch
# (contain PII + full DB copies — never commit)
backups/
.restore-drill/

# Stale editor/backup copies
*.bak

# Uploaded files (PII / photos) — never commit
media/
media_root/
